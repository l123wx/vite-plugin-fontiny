{
  "name": "example",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "npm run build && npm run preview",
    "build": "vite build",
    "preview": "vite preview --open"
  },
  "devDependencies": {
    "vite": "^5.3.3",
    "vite-plugin-inspect": "^0.8.1"
  }
}
